# Enable thumbnails for local files and streams
network=yes
audio=no

# Balanced thumbnail size for clarity vs performance
max_width=240
max_height=135

# More stable on weaker GPUs/decoders
hwdec=no

# Start worker early so first hover shows faster
spawn_first=yes
