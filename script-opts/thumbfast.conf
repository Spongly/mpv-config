# Enable thumbnails on local + network media
network=yes

# Sharper/larger preview thumbnails without going overboard
max_width=320
max_height=180

# Use safe hardware decoding path for thumbnail generation when possible
hwdec=yes

# Start thumbnail worker when opening files for snappier seek previews
spawn_first=yes
